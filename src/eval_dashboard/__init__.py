"""Interactive hand-tracking evaluation dashboard.

A local web app that runs the Strategy-3 control pipeline (`src.control.play`)
in the background -- driving the REAL mouse cursor and clicks -- while the
browser serves a three-obstacle course that measures how well a person can
click, hold tracking, and trace with the hand controller. See `server.py`.
"""
