# Core ML / training
torch>=2.10
torchvision>=0.25
timm>=1.0.19
numpy
pandas
scikit-learn
pyyaml
pillow
tqdm

# Data download (HaGRID range-request pulls)
remotezip

# Logging / EDA
tensorboard
matplotlib

# Experiment tracking + tuning (Week 3: MLflow store; Week 4: registry + Optuna)
mlflow>=3.0        # tracking store AND the model registry (./mlruns, file-backed)
optuna>=4.0        # TPE sampler for the src/tune.py hyper-parameter search
requests           # used by scripts/capture_endpoint_transcript.py to call the endpoint

# Real-time pipeline (Phases 4-5)
opencv-python
mediapipe
pydirectinput

# Export / inference (Phase 3+, AD-11)
onnx
onnxruntime
onnxscript        # torch>=2.6's ONNX exporter imports this; export fails without it

# Evaluation dashboard (src/eval_dashboard) -- local web obstacle course
flask
python-docx  # optional: real .docx reports; results.py falls back to .txt without it

# Notes:
# - Install torch/torchvision for your CUDA build from https://pytorch.org
#   (the RTX 4060 training box uses a CUDA wheel; this repo was also run CPU-only).
# - mediapipe is optional (AD-05): only needed if the bbox/MediaPipe cropper is used.
